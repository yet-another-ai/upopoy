#!/bin/bash -e

# If running the web server then create or migrate existing database.
if [[ "$*" == *"rails server"* || "$*" == *"falcon serve"* || "$*" == *"falcon host"* ]]; then
  ./bin/rails db:prepare
fi

exec "${@}"
